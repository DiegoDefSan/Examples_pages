const dinamicList = document.getElementsByTagName("p");
console.log(dinamicList);

const staticList = document.querySelectorAll("p");
console.log(staticList);

var newParagraph = document.createElement("p");